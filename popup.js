// Popup script - minimal, main functionality is in content script
document.addEventListener('DOMContentLoaded', () => {
  // Could add popup-specific functionality here if needed
  console.log('Amazon Order Exporter popup loaded');
});
